//! beater-browser-triage — scaffold (implementation in progress).

/// Crate placeholder; replaced by the implementation agent.
pub const CRATE_NAME: &str = "beater-browser-triage";

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn crate_name_is_set() {
        assert_eq!(CRATE_NAME, "beater-browser-triage");
    }
}
