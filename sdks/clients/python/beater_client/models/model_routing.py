# coding: utf-8

"""
    Beater API

    Agent observability, evaluation, gating, and human-review APIs for Beater

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from beater_client.models.model_routing_one_of import ModelRoutingOneOf
from beater_client.models.model_routing_one_of1 import ModelRoutingOneOf1
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

MODELROUTING_ONE_OF_SCHEMAS = ["ModelRoutingOneOf", "ModelRoutingOneOf1"]

class ModelRouting(BaseModel):
    """
    Per-request credential routing policy.  This enum is the encoding of *\"any model, optionally BYOK, else managed default\"*:  * [`ModelRouting::Byok`] — use the tenant's own opaque provider secret. The   model named in the [`ChatCompletionRequest`] is used as-is, so any model the   key can reach is reachable. * [`ModelRouting::Managed`] — use Beater's managed credentials with the given   default model. Only available when the gateway is built with a   [`ManagedDefault`] (hosted edition).  A [`GatewayRequest`] carries an *ordered* `Vec<ModelRouting>` so the gateway can fail over from one key/provider to the next. An empty list means *\"I did not bring a key\"*: the gateway then uses the managed default if configured, or returns [`GatewayError::NoKeyAndNoManagedDefault`] in OSS.
    """
    # data type: ModelRoutingOneOf
    oneof_schema_1_validator: Optional[ModelRoutingOneOf] = None
    # data type: ModelRoutingOneOf1
    oneof_schema_2_validator: Optional[ModelRoutingOneOf1] = None
    actual_instance: Optional[Union[ModelRoutingOneOf, ModelRoutingOneOf1]] = None
    one_of_schemas: Set[str] = { "ModelRoutingOneOf", "ModelRoutingOneOf1" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = ModelRouting.model_construct()
        error_messages = []
        match = 0
        # validate data type: ModelRoutingOneOf
        if not isinstance(v, ModelRoutingOneOf):
            error_messages.append(f"Error! Input type `{type(v)}` is not `ModelRoutingOneOf`")
        else:
            match += 1
        # validate data type: ModelRoutingOneOf1
        if not isinstance(v, ModelRoutingOneOf1):
            error_messages.append(f"Error! Input type `{type(v)}` is not `ModelRoutingOneOf1`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in ModelRouting with oneOf schemas: ModelRoutingOneOf, ModelRoutingOneOf1. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in ModelRouting with oneOf schemas: ModelRoutingOneOf, ModelRoutingOneOf1. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into ModelRoutingOneOf
        try:
            instance.actual_instance = ModelRoutingOneOf.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into ModelRoutingOneOf1
        try:
            instance.actual_instance = ModelRoutingOneOf1.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into ModelRouting with oneOf schemas: ModelRoutingOneOf, ModelRoutingOneOf1. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into ModelRouting with oneOf schemas: ModelRoutingOneOf, ModelRoutingOneOf1. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], ModelRoutingOneOf, ModelRoutingOneOf1]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


